namespace Noto.Infrastructure;

/// <summary>
/// Anchors this assembly for reflection in architecture tests (#20).
/// </summary>
/// <remarks>
/// SQLite, repositories and the migration runner arrive in #8 (ADR-003).
/// </remarks>
public static class AssemblyMarker;
