namespace Noto.UseCases;

/// <summary>
/// Anchors this assembly for reflection in architecture tests (#20).
/// </summary>
/// <remarks>
/// Commands, handlers and events arrive in #21 (ADR-010). Until then this
/// project exists to hold the boundary rather than to hide an empty
/// implementation.
/// </remarks>
public static class AssemblyMarker;
